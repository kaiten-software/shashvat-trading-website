import { MapPin, Phone, Mail, Calendar } from "lucide-react";
import { useEffect } from "react";

export default function ContactSection() {
  useEffect(() => {
    // Load Calendly widget script
    const script = document.createElement('script');
    script.src = 'https://assets.calendly.com/assets/external/widget.js';
    script.async = true;
    document.body.appendChild(script);

    return () => {
      document.body.removeChild(script);
    };
  }, []);

  return (
    <section className="py-24 md:py-32 bg-gradient-to-b from-blue-50 to-white dark:from-blue-950/30 dark:to-background" id="contact">
      <div className="max-w-7xl mx-auto px-4 md:px-6 lg:px-8">
        <div className="text-center mb-16">
          <h2 className="text-4xl md:text-5xl font-light text-foreground mb-6" data-testid="text-contact-headline">
            Get In Touch
          </h2>
          <p className="text-lg md:text-xl text-muted-foreground" data-testid="text-contact-subheadline">
            Experience Japanese Purity. Experience Shizensui.
          </p>
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16">
          {/* Contact Information */}
          <div className="space-y-8">
            <div>
              <h3 className="text-2xl font-semibold text-foreground mb-6">Shizensui India</h3>
              <div className="space-y-6">
                <div className="flex items-start gap-4">
                  <div className="mt-1 text-blue-600 dark:text-blue-400">
                    <MapPin className="w-6 h-6" />
                  </div>
                  <div>
                    <p className="font-medium text-foreground mb-1">Visit Us</p>
                    <p className="text-muted-foreground leading-relaxed">
                      Gulabi Nagar, C-54/55, Ajmer Rd,<br />
                      Uma Colony, Sushilpura, Sodala,<br />
                      Jaipur, Rajasthan 302006
                    </p>
                  </div>
                </div>

                <div className="flex items-start gap-4">
                  <div className="mt-1 text-blue-600 dark:text-blue-400">
                    <Phone className="w-6 h-6" />
                  </div>
                  <div>
                    <p className="font-medium text-foreground mb-1">Call Us</p>
                    <p className="text-muted-foreground">+91 (Contact Number)</p>
                  </div>
                </div>

                <div className="flex items-start gap-4">
                  <div className="mt-1 text-blue-600 dark:text-blue-400">
                    <Mail className="w-6 h-6" />
                  </div>
                  <div>
                    <p className="font-medium text-foreground mb-1">Email Us</p>
                    <p className="text-muted-foreground">info@shizensui-india.com</p>
                  </div>
                </div>
              </div>
            </div>

            <div className="p-6 bg-gradient-to-br from-blue-100 to-cyan-100 dark:from-blue-900/30 dark:to-cyan-900/30 rounded-xl border border-blue-200 dark:border-blue-800">
              <p className="text-sm text-foreground/80 italic">
                "Bringing 45+ years of Japanese water purification excellence to India. Join us in experiencing the future of healthy hydration."
              </p>
            </div>

            {/* Schedule Meeting Button */}
            <Button 
              onClick={openCalendlyPopup}
              className="w-full h-14 text-lg bg-gradient-to-r from-blue-600 to-cyan-600 hover:from-blue-700 hover:to-cyan-700"
            >
              <Calendar className="w-5 h-5 mr-2" />
              Schedule a Meeting
            </Button>
          </div>

          {/* Contact Form or Calendly */}
          <div className="bg-white dark:bg-gray-900 p-8 rounded-2xl border border-blue-100 dark:border-blue-900 shadow-lg">
            {/* Toggle Tabs */}
            <div className="flex gap-4 mb-6 border-b border-gray-200 dark:border-gray-700">
              <button
                onClick={() => setShowCalendly(false)}
                className={`pb-3 px-4 font-medium transition-colors ${
                  !showCalendly 
                    ? 'text-blue-600 dark:text-blue-400 border-b-2 border-blue-600 dark:border-blue-400' 
                    : 'text-muted-foreground hover:text-foreground'
                }`}
              >
                Send Message
              </button>
              <button
                onClick={() => setShowCalendly(true)}
                className={`pb-3 px-4 font-medium transition-colors ${
                  showCalendly 
                    ? 'text-blue-600 dark:text-blue-400 border-b-2 border-blue-600 dark:border-blue-400' 
                    : 'text-muted-foreground hover:text-foreground'
                }`}
              >
                <Calendar className="w-4 h-4 inline mr-2" />
                Schedule Meeting
              </button>
            </div>

            {!showCalendly ? (
              <form className="space-y-6" onSubmit={(e) => { e.preventDefault(); console.log('Form submitted'); }}>
                <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                  <Input 
                    placeholder="First Name" 
                    className="h-12"
                    data-testid="input-firstname"
                    required
                  />
                  <Input 
                    placeholder="Last Name" 
                    className="h-12"
                    data-testid="input-lastname"
                    required
                  />
                </div>
                
                <Input 
                  type="email" 
                  placeholder="Email Address" 
                  className="h-12"
                  data-testid="input-email"
                  required
                />
                
                <Input 
                  type="tel" 
                  placeholder="Phone Number" 
                  className="h-12"
                  data-testid="input-phone"
                />
                
                <Textarea 
                  placeholder="Your Message" 
                  className="min-h-[150px] resize-none"
                  data-testid="textarea-message"
                  required
                />
                
                <Button 
                  type="submit" 
                  className="w-full h-12 text-lg"
                  data-testid="button-submit"
                >
                  Send Message
                </Button>
              </form>
            ) : (
              <div 
                className="calendly-inline-widget" 
                data-url="https://calendly.com/kaiten/koristu"
                style={{ minWidth: '320px', height: '630px' }}
              />
            )}
          </div>
        </div>
      </div>
    </section>
  );
}
